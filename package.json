{
  "name": "gitmop",
  "version": "0.1.0",
  "description": "Find and delete stale local git branches.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "gitmop": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test",
    "prepublishOnly": "npm test"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ulot2/gitmop.git"
  }
}
